add_namespace = philippines

# Duterte-era federalist shift: the Partido Federal ng Pilipinas takes the Conservative slot.
# Idempotent and hidden; scheduled from the ETD year hook (forward play) and the startup
# catch-up (mid-game starts) in common/scripted_effects/00_yearly_effects.txt.
country_event = {
	id = philippines.1
	hidden = yes
	is_triggered_only = yes
	trigger = { NOT = { has_country_flag = PHI_formation_of_the_federals } }

	immediate = {
		log = "[GetDateText]: [This.GetName]: philippines.1 - Partido Federal ng Pilipinas takes the Conservative slot"
		set_country_flag = PHI_formation_of_the_federals
		hidden_effect = { update_party_name = yes }
	}
}
