antarctica_post_treaty_activity_category = {
	antarctica_post_treaty_resource_extraction = {
		icon = tungsten
		available = { NOT = { has_idea = ANT_illegal_extraction_idea } }
		cost = 50
		days_remove = 90
		fixed_random_seed = no

		complete_effect = {
			set_temp_variable = { treasury_change = -10 }
			modify_treasury_effect = yes
			add_stability = -0.02
			random_owned_state = {
				random_list = {
					33 = {
						add_resource = {
							type = chromium
							amount = 2
						}
					}
					33 = {
						add_resource = {
							type = tungsten
							amount = 2
						}
					}
					33 = {
						add_resource = {
							type = aluminium
							amount = 2
						}
					}
				}
			}
			add_timed_idea = { idea = ANT_illegal_extraction_idea days = 60 }
			set_temp_variable = { antarctica_station_reports_temp = 1 }
			increase_antarctica_station_reports = yes
			for_each_scope_loop = {
				array = global.atcp_members
				add_opinion_modifier = {
					target = ROOT
					modifier = antarctica_modifier_small_consequences
				}
			}
			log = "[GetDateText]: [Root.GetName]: Decision antarctica_post_treaty_resource_extraction"
		}

		ai_will_do = {
			base = 1
		}
	}

	antarctica_post_treaty_military_preparations = {
		icon = army_reform
		available = { NOT = { has_idea = ANT_military_training_idea } }
		cost = 75
		days_remove = 120

		complete_effect = {
			army_experience = 10
			add_war_support = 0.03
			add_stability = -0.02
			add_threat = 0.5
			add_timed_idea = { idea = ANT_military_training_idea days = 90 }
			set_temp_variable = { antarctica_station_reports_temp = 2 }
			increase_antarctica_station_reports = yes
			for_each_scope_loop = {
				array = global.atcp_members
				add_opinion_modifier = {
					target = ROOT
					modifier = antarctica_modifier_medium_consequences
				}
			}
			log = "[GetDateText]: [Root.GetName]: Decision antarctica_post_treaty_military_preparations"
		}

		ai_will_do = {
			base = 1
		}
	}
}
