FSA_internal_revolt_category = {

	FSA_defection_to_extremist = {
		icon = GFX_decision_revolt

		available = {
			hidden_trigger = { always = no }
		}

		war_with_on_timeout = NUS

		days_mission_timeout = 140
		is_good = yes
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_government = fascism }
		}

		timeout_effect = {
			if = {
				limit = { has_idea = FSA_extremism4 }
				swap_ideas = {
					remove_idea = FSA_extremism4
					add_idea = FSA_extremism5
				}
			}
			else_if = {
				limit = { has_idea = FSA_extremism3 }
				swap_ideas = {
					remove_idea = FSA_extremism3
					add_idea = FSA_extremism4
				}
			}
			else_if = {
				limit = { has_idea = FSA_extremism2 }
				swap_ideas = {
					remove_idea = FSA_extremism2
					add_idea = FSA_extremism3
				}
			}
			else_if = {
				limit = { has_idea = FSA_extremism1 }
				swap_ideas = {
					remove_idea = FSA_extremism1
					add_idea = FSA_extremism2
				}
			}
			else = {
				activate_mission = FSA_defection_to_extremist
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_aleppo = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 566
			controls_province = 12473
		}

		days_mission_timeout = 120
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_aleppo }
		}

		timeout_effect = { set_country_flag = FSA_capture_aleppo }

		complete_effect = {
			set_country_flag = FSA_capture_aleppo
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_ar_raqqah = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 191
			controls_province = 3127
		}

		days_mission_timeout = 500
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_ar_raqqah }
		}

		timeout_effect = { set_country_flag = FSA_capture_ar_raqqah }

		complete_effect = {
			set_country_flag = FSA_capture_ar_raqqah
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_damascus = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 186
			controls_province = 4111
		}

		days_mission_timeout = 600
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_damascus }
		}

		timeout_effect = { set_country_flag = FSA_capture_damascus }

		complete_effect = {
			set_country_flag = FSA_capture_damascus
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_deir_ez_zur = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 192
			controls_province = 6312
		}

		days_mission_timeout = 550
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_deir_ez_zur }
		}

		timeout_effect = { set_country_flag = FSA_capture_deir_ez_zur }

		complete_effect = {
			set_country_flag = FSA_capture_deir_ez_zur
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_hama = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 571
			controls_province = 14599
		}

		days_mission_timeout = 410
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_hama }
		}

		timeout_effect = { set_country_flag = FSA_capture_hama }

		complete_effect = {
			set_country_flag = FSA_capture_hama
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_homs = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 188
			controls_province = 6302
		}

		days_mission_timeout = 440
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_homs }
		}

		timeout_effect = { set_country_flag = FSA_capture_homs }

		complete_effect = {
			set_country_flag = FSA_capture_homs
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_latakia = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 189
			controls_province = 1088
		}

		days_mission_timeout = 900
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_latakia }
		}

		timeout_effect = { set_country_flag = FSA_capture_latakia }

		complete_effect = {
			set_country_flag = FSA_capture_latakia
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}

	FSA_capture_tartus = {
		icon = GFX_decision_generic_operation

		available = {
			controls_state = 189
			controls_province = 10087
		}

		days_mission_timeout = 950
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = { has_government = fascism }
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = { has_country_flag = FSA_capture_tartus }
		}

		timeout_effect = { set_country_flag = FSA_capture_tartus }

		complete_effect = {
			set_country_flag = FSA_capture_tartus
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}
	}
}
