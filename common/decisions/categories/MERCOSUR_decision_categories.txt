MER_mercosur_diplomacy = {
	# Keep this list in sync with global.mercosur_potential in common/scripted_effects/00_startup_effects.txt.
	# allowed is locked at game start, so an array populated by startup effects cannot be used here.
	allowed = {
		OR = {
			original_tag = BRA
			original_tag = ARG
			original_tag = PAR
			original_tag = URG
			original_tag = BOL
			original_tag = CHL
			original_tag = COL
			original_tag = ECU
			original_tag = GUY
			original_tag = PRU
			original_tag = SUR
			original_tag = VEN
			original_tag = MEX
			original_tag = PAN
		}
	}
	icon = GFX_decision_category_generic_foreign_policy
	priority = 140

	visible = {
		OR = {
			has_dynamic_modifier = mercosur_member_state_modifier
			has_dynamic_modifier = unasul_member_state_modifier
			AND = {
				is_ai = no
				is_subject = no
				OR = {
					AND = {
						NOT = { has_idea = BRA_idea_associate_states }
						NOT = { has_dynamic_modifier = mercosur_member_state_modifier }
						any_of_scopes = {
							array = global.mercosur_member_state
							NOT = { tag = ROOT }
							is_subject = no
							has_at_least_regional_power_status = yes
							has_global_flag = MER_mercosur_requests_enabled
						}
					}
					AND = {
						has_idea = BRA_idea_associate_states
						NOT = { has_dynamic_modifier = mercosur_member_state_modifier }
						any_of_scopes = {
							array = global.mercosur_member_state
							NOT = { tag = ROOT }
							is_subject = no
							has_at_least_regional_power_status = yes
							has_global_flag = MER_mercosur_full_membership_enabled
						}
					}
					AND = {
						NOT = { tag = MEX }
						NOT = { tag = PAN }
						NOT = { has_dynamic_modifier = unasul_member_state_modifier }
						OR = {
							AND = {
								has_dynamic_modifier = mercosur_member_state_modifier
								has_global_flag = MER_unasul_established
							}
							has_global_flag = MER_unasul_expansion_enabled
						}
					}
				}
			}
		}
	}
}
