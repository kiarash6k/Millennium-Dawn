kasmir_war_decisions_category = {
	allowed = {
		OR = {
			original_tag = RAJ
			original_tag = PAK
		}
	}

	icon = GFX_decision_category_india
	picture = GFX_indian_conflict
	priority = 100
	visible = {
		country_exists = RAJ
		country_exists = PAK
		RAJ = { is_subject = no }
		PAK = { is_subject = no }
		NOT = { RAJ = { is_in_faction_with = PAK } }
	}
}

kasmir_war_china_decisions_category = {
	allowed = {
		OR = {
			original_tag = RAJ
			original_tag = CHI
		}
	}

	icon = GFX_decision_category_india
	picture = GFX_indian_conflict
	priority = 100
	visible = {
		country_exists = RAJ
		country_exists = CHI
		591 = { is_core_of = RAJ is_controlled_by = CHI }
		RAJ = { is_subject = no }
		CHI = { is_subject = no }
		NOT = { RAJ = { is_in_faction_with = CHI } }
	}
}
