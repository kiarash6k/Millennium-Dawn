# Author(s): AngriestBird
olv_production_speed_modifier = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

gnss_production_speed_modifier = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

comsat_production_speed_modifier = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

spysat_production_speed_modifier = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

killsat_production_speed_modifier = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

# Nuclear Fuel Production (base, flat kg/week — set by enrichment_facility buildings and content)
nuclear_reactor_fuel_production = {
	color_type = good
	value_type = number
	precision = 0
	category = country
}

# Nuclear Fuel Production Factor (percentage multiplier applied to the base)
nuclear_reactor_fuel_production_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}
